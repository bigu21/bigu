//-template(name="chat")

  //-+contentFor("headerTitle")
    //-h1.title Chats!

  //-+ionView
    //-+ionContent
      //-+ionList
        //-each chats
          //-+ionItem(path="chat.show" class="chatItem" avatar="true" buttonRight="true" iconRight="true")
              //-img(src="#{avatarUrl}")
              //-h2 #{name.first} #{name.last}
              //-p #{topic} 
              //-+ionIcon(icon="ios-arrow-forward" class="positive")

template(name="chatList")
  .page.chatList
    ul
      each chatList
        li 
          a(href="{{pathFor 'chatRoom' chatId=_id}}")
            img(src="#{avatarUrl}")
            span #{name.first} #{name.last} 
            .icon-right.icon-chat
